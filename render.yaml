services:
- type: web
  name: amfg-commission-calculator
  env: node
  plan: starter
  buildCommand: pnpm install --frozen-lockfile && pnpm run build
  startCommand: pnpm run start
  healthCheckPath: /api/scheduled/sync-status
  autoDeploy: true
